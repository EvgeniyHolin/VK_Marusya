import { ref } from 'vue'
import { defineStore } from 'pinia'

export const useRandomMovieStore = defineStore('RandomMovie', () => {
  const id = ref(0);
  const title = ref(0);
  const originalTitle = ref(0);
  const language = ref(0);
  const releaseYear = ref(0);
  const releaseDate = ref(0);
  const genres = ref([]);
  const plot = ref(0);
  const runtime = ref(0);
  const budget = ref(0);
  const revenue = ref(0);
  const homepage = ref(0);
  const status = ref(0);
  const posterUrl = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);
  const id = ref(0);


  return {  }
})
